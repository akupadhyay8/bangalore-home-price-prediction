# bangalore-home-price-prediction
A machine learning project that predicts home prices in Bangalore using a trained model. The project includes a Flask backend API, a frontend built with HTML, CSS, and JavaScript, and a dataset of housing information. The model is trained on the dataset and deployed for real-time predictions on a web interface.
